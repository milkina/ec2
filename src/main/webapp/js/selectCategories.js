$('select[multiple]').multiselect({
               columns: 2,
               placeholder: 'Select categories',
               selectAll : true,
               selectGroup:true,
               search:true
            });